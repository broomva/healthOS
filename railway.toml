[build]
builder = "nixpacks"
buildCommand = "bun install && bun lib/db/migrate.ts && bun run build"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
